---
---

<!-- ========== КОМПАКТНАЯ МОДАЛКА КАТЕГОРИЙ ==========
     Открывается по клику на любую кнопку с data-category в #examples.
     Контент заполняется JS-ом (src/scripts/examples.ts). -->
<dialog id="modal-examples" aria-labelledby="modal-examples-title" class="modal-scroll">
  <div class="modal-examples-card">
    <!-- Горизонталь — ступень panel, общая для всех модалок (docs/SPACING.md).
         Вертикаль полосы-шапки меньше: это не панель контента, а её край. -->
    <div class="flex items-start justify-between gap-4 px-panel py-5 border-b border-neutral-200">
      <div class="flex items-start gap-4">
        <div id="modal-examples-icon" class="w-14 h-14 bg-panel text-brand-blue flex items-center justify-center shrink-0"></div>
        <div>
          <h3 id="modal-examples-title" class="t-h-section t-h-section--compact t-on-brand"></h3>
          <div id="modal-examples-count" class="t-eyebrow t-on-muted mt-1"></div>
        </div>
      </div>
      <button type="button" id="modal-examples-close" aria-label="Закрыть"
              class="w-9 h-9 flex items-center justify-center bg-panel text-ink-dark hover:bg-ink-dark hover:text-white transition-colors duration-150 ease-brand shrink-0">
        <svg xmlns="http://www.w3.org/2000/svg" aria-hidden="true" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>

    <div class="p-panel">
      <ol id="modal-examples-list" class="space-y-2.5 px-3"></ol>
    </div>
  </div>
</dialog>

<script>
  import '../scripts/examples';
</script>
